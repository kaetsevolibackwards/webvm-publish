name: Deploy WebVM

on:
  workflow_dispatch:
    inputs:
      DOCKERFILE_PATH:
        description: "Dockerfile path"
        required: true
        default: "dockerfiles/debian_mini"

      IMAGE_SIZE:
        description: "Image size"
        required: true
        default: "750M"

      DEPLOY_TO_GITHUB_PAGES:
        description: "Deploy to GitHub Pages"
        required: true
        default: true

      GITHUB_RELEASE:
        description: "Upload GitHub release"
        required: true
        default: false


jobs:
  build:
    runs-on: ubuntu-latest

    env:
      TAG: webvm-image
      IMAGE_SIZE: ${{ github.event.inputs.IMAGE_SIZE }}
      WORKDIR: ${{ github.workspace }}/webvm_build

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup workspace
        run: |
          mkdir -p $WORKDIR
          echo "IMAGE_NAME=webvm_$(date +%Y%m%d)_${{ github.run_id }}.ext2" >> $GITHUB_ENV

      - name: Build Docker image
        run: |
          docker build . \
            --tag $TAG \
            --file ${{ github.event.inputs.DOCKERFILE_PATH }} \
            --platform=i386

      - name: Run container
        run: |
          docker run -d $TAG
          echo "CONTAINER_ID=$(docker ps -aq)" >> $GITHUB_ENV

      - name: Extract container config
        run: |
          echo "CMD=$(docker inspect $CONTAINER_ID | jq -c '.[0].Config.Cmd')" >> $GITHUB_ENV
          echo "ENV=$(docker inspect $CONTAINER_ID | jq -c '.[0].Config.Env')" >> $GITHUB_ENV
          echo "CWD=$(docker inspect $CONTAINER_ID | jq -c '.[0].Config.WorkingDir')" >> $GITHUB_ENV

      - name: Create ext2 image (FIXED MOUNT)
        run: |
          fallocate -l $IMAGE_SIZE $IMAGE_NAME
          mkfs.ext2 -F $IMAGE_NAME

          mkdir -p $WORKDIR/mnt
          sudo mount -o loop $IMAGE_NAME $WORKDIR/mnt

      - name: Copy filesystem into image
        run: |
          sudo docker cp -a $CONTAINER_ID:/ $WORKDIR/mnt/
          sudo umount $WORKDIR/mnt

      - name: Split image for hosting
        run: |
          split $IMAGE_NAME $WORKDIR/$IMAGE_NAME. -b 128k -d

      - name: Write metadata (FIXED PERMISSIONS)
        run: |
          SIZE=$(stat -c%s $IMAGE_NAME)
          echo $SIZE > $WORKDIR/$IMAGE_NAME.meta

      - name: Build frontend
        run: |
          npm install
          npm run build

      - name: Move build output
        run: |
          mv build/* $WORKDIR/

      - name: Generate index files
        run: |
          find $WORKDIR -type d | while read dir; do
            ls "$dir" > "$dir/index.list"
          done

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.WORKDIR }}

  deploy:
    needs: build
    if: ${{ github.event.inputs.DEPLOY_TO_GITHUB_PAGES == 'true' }}
    runs-on: ubuntu-latest

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}

    steps:
      - name: Deploy
        id: deploy
        uses: actions/deploy-pages@v4
